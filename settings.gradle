pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'CoinsEngine'

include 'NexEngine'
project(':NexEngine').projectDir = file('vendor/NexEngine')
